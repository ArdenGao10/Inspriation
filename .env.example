# 复制本文件为 .env，填入你的智谱 API Key。
# .env 已被 .gitignore 忽略，不会进入版本库。
# 申请地址：https://open.bigmodel.cn/usercenter/apikeys
#
# 注意：VITE_ 前缀的变量会被打进前端 bundle，用户可见 —— 仅用于本地开发方便。
# 生产环境真正藏 Key 需走后端（Vercel serverless / Supabase Edge Function）。
VITE_ZHIPU_KEY=

# 智谱模型（可选）。合成默认 glm-4-flash、对话默认 glm-4.5-flash，可分别覆盖：
# VITE_ZHIPU_MODEL=glm-4.6           # 备用强模型
# VITE_ZHIPU_SYNTH_MODEL=glm-4-flash # 首页摇一摇合成
# VITE_ZHIPU_CHAT_MODEL=glm-4.5-flash # 对话 Agent

# —— 部署到 Vercel 时 ——
# 前端【不要】配 VITE_ZHIPU_KEY（会被打进 bundle 暴露）。
# 改在 Vercel 项目 Settings → Environment Variables 配【服务端】变量（无 VITE_ 前缀，不进 bundle）：
#   ZHIPU_KEY=你的智谱key
# 前端没有 VITE_ZHIPU_KEY 时，会自动改调 /api/chat 代理，由服务端带 key 请求智谱。
# 本地开发仍用上面的 VITE_ZHIPU_KEY 直连（vite dev 不跑 serverless 函数）。

# 后续接 Supabase 时再填（现在留空）
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
